{
  "name": "domyvitalii",
  "version": "1.0.0",
  "description": "Domy v Itálii — Eleventy + Netlify + Stripe checkout for PDF",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "start": "eleventy --serve",
    "netlify": "netlify dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "stripe": "^18.5.0"
  }
}
